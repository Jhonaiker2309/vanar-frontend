const Navbar = () => {
  return (
    <header className="w-screen h-[101px] bg-[#1c1a23] opacity-80 absolute top-0 flex items-center justify-between px-8 z-50">
      <p>Logo</p>
      <button
        className="bg-white text-black font-semibold py-4 px-6 rounded-full"
        onClick={() => {}}
      >
        Connect Wallet
      </button>
    </header>
  );
};

export default Navbar;
